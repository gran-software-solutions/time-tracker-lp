---
import Footer from '../components/Footer.astro'
import Head from '../components/Head.astro';
---
<!doctype html>
<html xmlns="http://www.w3.org/1999/html" lang="en">
<Head title="Time Tracking for Software Developers | Timesheet Management">
    <meta name="description" content="Consulting Cockpit is a time tracking app designed for software developers, offering shortcuts, ctrl+k search bar, and visual widgets for efficient timesheet management. Start your 90-day free trial today.">
    <meta property="og:title" content="Consulting Cockpit: Time Tracking App for Software Developers">
    <meta property="og:description" content="Use shortcuts, ctrl+k search bar, and visual widgets to reduce time spent on timesheets. Start your 90-day free trial today.">
    <meta property="og:image" content="https://consultingcockpit.com/consultingcockpit-logo-compact.jpg">
    <meta property="og:url" content="https://consultingcockpit.com">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script type="application/ld+json">
        {
            "@context": "https://schema.org",
            "@type": "WebSite",
            "name": "Consulting Cockpit",
            "url": "https://consultingcockpit.com",
            "description": "Consulting Cockpit is a time tracking app designed for software developers, offering efficient timesheet management with shortcuts and visual widgets.",
            "author": {
                "@type": "Organization",
                "name": "GRAN Software Solutions GmbH"
            }
        }
    </script>
    <!-- Google tag (gtag.js) -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-RJB0Z11MPP"></script>
    <script>
        window.dataLayer = window.dataLayer || [];

        function gtag() {
            dataLayer.push(arguments);
        }

        gtag('js', new Date());
        gtag('config', 'G-RJB0Z11MPP');

        const converters = document.querySelectorAll('.convert');
        converters.forEach(el => {
            el.addEventListener('click', it => {
                let url = it.target.href
                var callback = function () {
                    if (typeof (url) != 'undefined') {
                        window.location = url;
                    }
                };

                gtag('event', 'conversion', {
                    'send_to': 'AW-11039123167/E1YkCPPM_4AaEN_N7o8p',
                    'event_callback': callback
                });

                return false;
            });
        });
    </script>
    <script>
    document.addEventListener('DOMContentLoaded', function() {
    const notificationDiv = document.getElementById('notification');

    const form = document.querySelector('.signup-form');
    form.addEventListener('submit', function(e) {
        e.preventDefault();

        const nameInput = document.getElementById('name');
        const emailInput = document.getElementById('email');

        const data = {
            name: nameInput.value,
            email: emailInput.value
        };

        fetch('http://localhost:8080/leads', {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json'
            },
            body: JSON.stringify(data)
        })
        .then(response => {
            if (response.status === 200) {
                showNotification('Great! We\'ll be in touch soon. 🚀', 'success');
                document.getElementById('signup-form').remove();
                form.reset();
            } else if (response.status === 400) {
                showNotification('Could not send your application. Please check your e-mail address!', 'error');
            } else if (response.status === 500) {
                showNotification('Our servers are taking a coffee break. Mind trying again later?', 'error');
            } else {
                showNotification('Something went sideways. Care to try again?', 'error');
            }
        })
        .catch(error => {
            showNotification('Couldn\'t reach our servers. Internet acting up?', 'error');
            console.error('Error:', error);
        });
    });

    function showNotification(message, type) {
        const notificationDiv = document.getElementById('notification');
        notificationDiv.textContent = message;
        notificationDiv.style.display = 'block';
    }
});
    </script>

    <style type="text/css">
        #ribbon::after {
            background: #fff9ef;
            content: "";
            height: 45%;
            left: 0;
            position: absolute;
            top: 85%;
            transform: skewY(10deg);
            width: 100%;
            z-index: -1;
        }
    </style>
    <!-- Google Tag Manager -->
    <script>(function (w, d, s, l, i) {
        w[l] = w[l] || [];
        w[l].push({
            'gtm.start':
                new Date().getTime(), event: 'gtm.js'
        });
        var f = d.getElementsByTagName(s)[0],
            j = d.createElement(s), dl = l != 'dataLayer' ? '&l=' + l : '';
        j.async = true;
        j.src =
            'https://www.googletagmanager.com/gtm.js?id=' + i + dl;
        f.parentNode.insertBefore(j, f);
    })(window, document, 'script', 'dataLayer', 'GTM-TGFDCQSQ');</script>
    <!-- End Google Tag Manager -->

<body>
<!-- Google Tag Manager (noscript) -->
<noscript>
    <iframe src="https://www.googletagmanager.com/ns.html?id=GTM-TGFDCQSQ"
            height="0" width="0" style="display:none;visibility:hidden"></iframe>
</noscript>
<!-- End Google Tag Manager (noscript) -->
    <div class=wrapper>
        <div class="container">
            <img src="https://app.consultingcockpit.com/assets/Consulting_cockpit_logo_peach-BMG-cAIO.webp" alt="Consulting Cockpit Logo" class="logo" />

            <h1>THE FUTURE OF CONSULTING BUSINESS<br/>IS BRIGHT</h1>

    <p>
    We’re inviting <b>100 bold founders</b> to join the <b>Early Access Program</b> for the Consulting Cockpit - a new kind of control center for software agencies.
    </p>

    <p>
    This isn’t just another SaaS tool.
    It’s a co-created journey with pioneers like you who want to challenge the status quo and build the future of consulting.
    </p>

    <h2>WHAT IS THE CONSULTING COCKPIT?</h2>
    <p>
    It's a suite of tools to meet all the needs for a software consulting business.
    It includes standard features like time tracking and timesheets, invoices, leave management and contracts management,
    as well as advanced functions to capture satisfaction of your clients and consultants.
    </p>

    <div class="gallery">
        <img src="app1.png" alt="Image 1">
        <img src="app2.png" alt="Image 2">
        <img src="app3.png" alt="Image 3">
    </div>

    <h2>HOW DOES IT CHANGE THE WAY I RUN MY CONSULTING BUSINESS?</h2>
    <p>
    The Consulting Cockpit learns your business as you feed it with data. It recognises patterns, identifies weak spots
    and opportunities. Then it presents you all the important KPIs for your consulting business but also sends
    notifications with dangers and opportunities.
    </p>

    <div class="carousel" id="carousel">
    <div class="carousel-track" id="carousel-track">
    <!-- Images will be inserted here by JS -->
    </div>
    <div class="buttons">
    <button id="prev">&#10094;</button>
    <button id="next">&#10095;</button>
    </div>
    </div>

    <!-- Modal viewer -->
    <div id="modal" style="
    display: none;
    position: fixed;
    top: 0; left: 0;
    width: 100%; height: 100%;
    background-color: rgba(0, 0, 0, 0.9);
    justify-content: center;
    align-items: center;
    z-index: 1000;
    ">
    <img id="modal-img" src="" style="max-width: 90%; max-height: 90%; border-radius: 8px;">
    </div>

    <h2>WHY EARLY ACCESS?</h2>
    <p>
    Because Early Access means more than early features.
    It means your voice shapes what we’re building.
    You’ll get:
    </p>
    <ul>
        <li>Exclusive early access to the platform before anyone else</li>
        <li>A private Discord community of visionary founders</li>
        <li>The chance to influence features and the roadmap</li>
    </ul>

    <h2>WHO SHOULD APPLY FOR THE EARLY ACCESS?</h2>
    <p>
    You're a perfect fit if you:
    <ul>
    <li>Run a software agency</li>
    <li>Employ up to 20 people or contractors</li>
    <li>Are ready to shape the future of consulting</li>
    </ul>

    </p>
    <p>
        Apply for an interview and secure your spot in the private Discord community of pioneering agency founders.
    </p>
        <div id="notification">

        </div>

        <form id="signup-form" class="signup-form">
            <input type="text" id="name" placeholder="Your first name e.g. John" required />
            <input type="email" id="email" placeholder="Your E-Mail address e.g. jd@mydomain.com" required />

            <div class="checkbox">
                <input type="checkbox" id="terms" required />
                <label for="terms">I accept Consulting Cockpit’s privacy policy (<a href="/datenschutz">found here</a>).</label>
            </div>

            <button type="submit">Apply for Early Access</button>
        </form>
    </div>
</div>

    <Footer/>


    <script>
    const track = document.getElementById('carousel-track');
    const images = Array.from({ length: 10 }, (_, i) => `${i + 1}.png`);

    images.forEach(src => {
    const slide = document.createElement('div');
    slide.className = 'carousel-slide';
    slide.innerHTML = `<img src="${src}" alt="Image">`;
    track.appendChild(slide);
});

    let index = 0;

    const updateCarousel = () => {
    track.style.transform = `translateX(-${index * 100}%)`;
};

    document.getElementById('next').addEventListener('click', () => {
    index = (index + 1) % images.length;
    updateCarousel();
});

    document.getElementById('prev').addEventListener('click', () => {
    index = (index - 1 + images.length) % images.length;
    updateCarousel();
});

    // Swipe support
    let startX = 0;
    let isDown = false;

    track.addEventListener('touchstart', (e) => {
    startX = e.touches[0].clientX;
    isDown = true;
});

    track.addEventListener('touchmove', (e) => {
    if (!isDown) return;
    const diff = e.touches[0].clientX - startX;
    if (Math.abs(diff) > 50) {
    if (diff > 0) {
        index = (index - 1 + images.length) % images.length;
    } else {
        index = (index + 1) % images.length;
    }
    updateCarousel();
    isDown = false;
}
});

    track.addEventListener('touchend', () => {
    isDown = false;
});

    // Modal logic
    const modal = document.getElementById('modal');
    const modalImg = document.getElementById('modal-img');

    document.querySelectorAll('img').forEach(img => {
    console.log(img)
    img.style.cursor = 'zoom-in';
    img.addEventListener('click', () => {
        modalImg.src = img.src;
        modal.style.display = 'flex';
    });
});

    modal.addEventListener('click', () => {
    modal.style.display = 'none';
});

    </script>

    </Head>
</body>
</html>